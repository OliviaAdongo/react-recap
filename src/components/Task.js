import React from 'react'
// import PropTypes from 'prop-types'

const tasks = {
    id: 1,
    text: 'Shopping List',
    day: "Feb 5th",
    reminder: true,
}

const Task = props => {
  return (
    <div>
    
    
    </div>
  )
}

// Task.propTypes = {}

export default Task